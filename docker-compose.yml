version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "7001:8000"
    volumes:
      - ./backend:/app
    environment:
      - PYTHONUNBUFFERED=1

  frontend:
    build: ./frontend
    ports:
      - "7002:80"
    depends_on:
      - backend
